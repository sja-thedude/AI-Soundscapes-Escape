# MusicGen + LoRA Training Requirements
# For A100 GPU with 48GB RAM

# Core PyTorch (CUDA 11.8 for A100)
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0

# Transformers and MusicGen
transformers>=4.30.0
accelerate>=0.20.0
datasets>=2.12.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.10.0

# Data handling
pandas>=1.5.0
numpy>=1.21.0

# Training utilities
tqdm>=4.64.0
wandb>=0.15.0

# Optional: For better performance
flash-attn>=2.0.0  # Flash Attention for faster training
xformers>=0.0.20   # Memory efficient attention

# System utilities
psutil>=5.9.0
